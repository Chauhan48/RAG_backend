<!DOCTYPE html>
<html>
<head>
  <title>Signup</title>
  <style>
    body {
      font-family: 'Segoe UI', Arial, sans-serif;
      background: linear-gradient(120deg, #e0eafc 0%, #cfdef3 100%);
      min-height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .signup-container {
      background: #fff;
      padding: 36px 40px 28px 40px;
      border-radius: 16px;
      box-shadow: 0 4px 32px rgba(52, 152, 219, 0.10);
      max-width: 420px;
      width: 100%;
      margin: 32px auto;
      border: 1px solid #e3e3e3;
    }

    h1 {
      text-align: center;
      margin-bottom: 28px;
      letter-spacing: 2px;
      color: #3498db;
      font-weight: 700;
    }

    label {
      display: block;
      margin-top: 18px;
      margin-bottom: 5px;
      font-size: 1rem;
      color: #217dbb;
      font-weight: 600;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 11px 17px;
      border: 2px solid #cfd8dc;
      border-radius: 7px;
      font-size: 1rem;
      background: #f7faff;
      color: #222;
      margin-bottom: 14px;
      transition: border-color 0.3s, box-shadow 0.3s;
      box-sizing: border-box;
    }

    input:focus {
      border-color: #3498db;
      background: #fff;
      outline: none;
      box-shadow: 0 0 8px rgba(52, 152, 219, 0.13);
    }

    button {
      margin-top: 26px;
      padding: 12px;
      width: 100%;
      background: linear-gradient(120deg, #3498db 70%, #34db6e 100%);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      box-shadow: 0 2px 9px rgba(52, 152, 219, 0.07);
      transition: background 0.2s, box-shadow 0.2s;
    }
    button:hover {
      background: linear-gradient(120deg, #217dbb 70%, #2ecc71 100%);
      box-shadow: 0 4px 16px rgba(52, 152, 219, 0.12);
    }

    .message {
      text-align: center;
      font-size: 1.07rem;
      margin: 8px 0 0 0;
      padding: 5px 0;
      border-radius: 6px;
      width: 100%;
      font-weight: 500;
    }
    .errorMsg { background: #ffd6d6; color: #c0392b; }
    .successMsg { background: #daf7dc; color: #27ae60; }

    .login-link {
      text-align: center;
      margin-top: 20px;
      font-size: 1rem;
      color: #555;
    }
    .login-link a {
      color: #3498db;
      text-decoration: none;
      font-weight: 600;
      margin-left: 4px;
    }
    .login-link a:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <div class="signup-container">
    <h1>Sign Up</h1>
    
    <% if (errorMsg) { %>
      <div class="message errorMsg"><%= errorMsg %></div>
    <% } %>
    <% if (successMsg) { %>
      <div class="message successMsg"><%= successMsg %></div>
    <% } %>

    <form action="/user/signup" method="POST">
      <label for="firstName">First Name</label>
      <input type="text" id="firstName" name="firstName" required minlength="2" maxlength="20" />

      <label for="lastName">Last Name</label>
      <input type="text" id="lastName" name="lastName" required minlength="2" maxlength="20" />

      <label for="email">Email</label>
      <input type="email" id="email" name="email" required />

      <label for="password">Password</label>
      <input type="password" id="password" name="password" required minlength="6" />

      <button type="submit">Sign Up</button>
    </form>

    <div class="login-link">
      Already have an account?
      <a href="/user/login">Login</a>
    </div>
  </div>
</body>
</html>
