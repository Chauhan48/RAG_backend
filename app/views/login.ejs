<!DOCTYPE html>
<html>
<head>
  <title>Login</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(120deg, #f0f4f8 0%, #d9e2ec 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0;
      padding: 20px;
    }

    .login-container {
      background: #ffffff;
      padding: 40px 36px 28px 36px;
      border-radius: 14px;
      box-shadow: 0 6px 25px rgba(52, 152, 219, 0.12);
      max-width: 400px;
      width: 100%;
      box-sizing: border-box;
      border: 1px solid #e3e7eb;
    }

    h1 {
      text-align: center;
      color: #336699;
      font-weight: 700;
      letter-spacing: 1.3px;
      margin-bottom: 28px;
      user-select: none;
    }

    label {
      display: block;
      font-weight: 600;
      margin-top: 18px;
      margin-bottom: 8px;
      color: #3a5068;
      font-size: 0.95rem;
    }

    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 12px 16px;
      border: 2px solid #b0bec5;
      border-radius: 7px;
      font-size: 1rem;
      background: #f7fbff;
      color: #293241;
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
      box-sizing: border-box;
      margin-top: 4px;
      outline-offset: 2px;
    }
    input[type="email"]:focus,
    input[type="password"]:focus {
      border-color: #336699;
      background: white;
      box-shadow: 0 0 8px rgba(51, 102, 153, 0.25);
      outline: none;
    }

    button {
      margin-top: 26px;
      padding: 14px;
      width: 100%;
      background: linear-gradient(120deg, #356abe 70%, #2a8de4 100%);
      border: none;
      border-radius: 9px;
      color: white;
      font-size: 1.1rem;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 3px 12px rgba(42, 141, 228, 0.4);
      transition: background 0.3s, box-shadow 0.3s;
    }
    button:hover {
      background: linear-gradient(120deg, #2a8de4 70%, #356abe 100%);
      box-shadow: 0 6px 22px rgba(42, 141, 228, 0.6);
    }

    .message {
      text-align: center;
      font-weight: 600;
      margin-top: 10px;
      padding: 10px;
      border-radius: 7px;
      font-size: 0.9rem;
      user-select: none;
    }
    .errorMsg {
      background: #ffd6d6;
      color: #b00020;
      border: 1px solid #b00020;
      margin-bottom: 10px;
    }
    .successMsg {
      background: #d4f8d4;
      color: #2f6627;
      border: 1px solid #2f6627;
      margin-bottom: 10px;
    }

    .register-link {
      text-align: center;
      margin-top: 18px;
      font-size: 1rem;
      color: #444;
      user-select: none;
    }
    .register-link a {
      color: #356abe;
      text-decoration: none;
      font-weight: 600;
      margin-left: 5px;
    }
    .register-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>Login</h1>

    <% if (errorMsg) { %>
      <div class="message errorMsg"><%= errorMsg %></div>
    <% } %>
    <% if (successMsg) { %>
      <div class="message successMsg"><%= successMsg %></div>
    <% } %>

    <form action="/user/login" method="POST">
      <label for="email">Email</label>
      <input type="email" id="email" name="email" required />

      <label for="password">Password</label>
      <input type="password" id="password" name="password" required minlength="6" />

      <button type="submit">Login</button>
    </form>

    <p class="register-link">
      New user?
      <a href="/">Register</a>
    </p>
  </div>
</body>
</html>
